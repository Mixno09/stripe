{% extends 'base.html.twig' %}

{% block title %}Success{% endblock %}

{% block body %}
    <body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-4">
                <div class="card">
                    <div class="card-body">
                            <div
                                style="color: green;
                                                border: 2px green solid;
                                                text-align: center;
                                                padding: 5px;margin-bottom: 10px;">
                                {{ status }}
                            </div>
                        <div class="form-control">
                            <a href="{{ path('app_stripe') }}">На главную</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </body>
{% endblock %}
